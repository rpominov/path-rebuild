{
  "name": "path-rebuild",
  "version": "0.0.1",
  "description": "Transform a file path by splitting the original path into an array of parts and defining a new path in terms of indices of the parts",
  "main": "./PathRebuild.bs.js",
  "scripts": {
    "build": "rescript",
    "clean": "rescript clean -with-deps",
    "start": "rescript build -w"
  },
  "keywords": [
    "rescript",
    "path",
    "transform",
    "rebuild"
  ],
  "contributors": [
    "Roman Pominov"
  ],
  "homepage": "https://github.com/rpominov/path-rebuild#readme",
  "bugs": "https://github.com/rpominov/path-rebuild/issues",
  "repository": "github:rpominov/path-rebuild",
  "license": "MIT",
  "dependencies": {
    "rescript": "^9.1.4"
  }
}
